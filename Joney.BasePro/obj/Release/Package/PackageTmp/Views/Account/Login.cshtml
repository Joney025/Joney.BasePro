
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Login</title>
    <script>
        if (window != top) {
            top.location.href = location.href;
        }
        // onload="return reloadRootPage();"
        function reloadRootPage() {
            //var top, eWin = window;
            //try {
            //    while (eWin.location.host == eWin.parent.location.host && eWin.location.href != eWin.parent.location.href) {
            //        eWin = eWin.parent;
            //    }
            //} catch (e) {
            //    top = eWin;
            //}
            //var win = window;
            //while (win != win.parent) {
            //    win = win.parent;
            //}
            //return win;
        }
        function CheckLogin() {
            if (true) {
                return true;
            } else {
                return false;
            }
        }
    </script>
</head>
<body>
    <div>
        <form action="LogOn" method="post" >
            @Html.AntiForgeryToken()
            <div>
                <ul>
                    <li><input type="text" id="UserName" name="UserName" value="admin" placeholder="用户名" ></li>
                    <li></li>
                    <li><input type="password" id="Password" name="Password" value="123456" placeholder="用户密码" ></li>
                    <li></li>
                    <li><button type="submit" tabindex="4" class="btn-submit">登 录</button></li>
                </ul>
            </div>
            <div class="control-group" style="text-align:center;display:none;">
                <span id="errorMsg" style="color:#ffffff;">@Html.ValidationMessage("loginMsgValidator")</span>
                @Html.ActionLink("忘记密码？", "Register", "Account")
                @Html.ActionLink("[注册]", "Register", "Account")
            </div>
        </form>
    </div>
</body>
</html>
